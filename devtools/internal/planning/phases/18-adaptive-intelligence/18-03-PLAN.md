---
phase: 18
plan: "18-03"
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - TypeFlow/Services/PromptBuilder.swift
requirements: []
autonomous: true
---

# Phase 18 Gap Plan: Lexicon Hallucination Fix

<objective>
Fix an issue where the model hallucinates by treating the custom vocabulary as a list to be explicitly recited instead of using it as a stylistic influence. Update the system instructions in `PromptBuilder.swift` to add a strict negative constraint.
</objective>

<verification>
## Verification Criteria
- `PromptBuilder.swift` contains the exact negative constraint provided: "CRITICAL: You are an invisible autocomplete engine. DO NOT recite, list, or explicitly mention the provided vocabulary words. Only use them naturally if they flawlessly fit the immediate grammatical context of the suffix. Prioritize the user's document context above all else."
- The system instructions are clearly separated from the User Text to prevent the model from blending them.
</verification>

<must_haves>
- Add the strict negative constraint to the prompt.
- Ensure proper separation between system instructions and user text.
</must_haves>

<tasks>
<task>
<read_first>
- TypeFlow/Services/PromptBuilder.swift
</read_first>
<action>
1. Open `PromptBuilder.swift`.
2. Locate the `buildStaticPrefix` function where the `UserCustomLexicon` is injected into the prompt.
3. Replace the existing instruction logic for the custom lexicon with the new exact negative constraint.
4. Ensure the prompt formatting has clear delimiters separating the system instructions from the context/user text (e.g., using explicit section headers or blank lines).
</action>
<acceptance_criteria>
- `PromptBuilder.swift` contains the updated negative constraint.
</acceptance_criteria>
</task>
</tasks>
