---
plan: 260607-x93
phase: quick
status: pending
dependencies: []
---

# Quick Plan: Fix compiler warnings in AppDelegate, LLMEngine, and ScreenContextManager

## Objective
Fix 4 compiler warnings across the project: unused do-catch blocks for non-throwing functions, unnecessary `try` keywords, and deprecated `CGWindowListCreateImage` usage.

## Tasks

### 1. Fix AppDelegate Warning
- **Files:** `TypeFlow/AppDelegate.swift`
- **Action:** [MODIFY] Remove the `do-catch` block wrapping `TypeFlowShortcuts.updateAppShortcutParameters()` inside the `Task`.
- **Verify:** `updateAppShortcutParameters()` is called directly inside the Task.
- **Done:** [ ]

### 2. Fix LLMEngine Warnings
- **Files:** `TypeFlow/Services/LLMEngine.swift`
- **Action:** [MODIFY] Line 112: Remove `try` before `await container.perform`. Line 144: Remove the `do-catch` wrapping the pre-warm block.
- **Verify:** Unreachable catch warnings are eliminated.
- **Done:** [ ]

### 3. Refactor ScreenContextManager with ScreenCaptureKit
- **Files:** `TypeFlow/Services/ScreenContextManager.swift`
- **Action:** [MODIFY] Import `ScreenCaptureKit`. Replace `CGWindowListCreateImage` with `SCShareableContent.current` and `SCScreenshotManager.captureImage(contentFilter:configuration:)` to capture the primary display asynchronously.
- **Verify:** The deprecation warning for macOS 14 is resolved and OCR still receives a valid CGImage.
- **Done:** [ ]
